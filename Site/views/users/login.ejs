<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head', {title:"Login", namePage:"login"}) %>

    <body>
        <%- include('../partials/header') %>

            <main class="login">
                <form action="/users/login" method="POST">
                    <h2>INICIAR SESION</h2>

                    <div class="mainlogin">
                        <div class="main-input">
                            <label for="user">Correo Electrónico</label> 
                            <input type="text" name="email" id="email">
                        </div>
                        <% if(typeof(errors) != 'undefined' && errors.email){%>
                            <span class="text-error">
                              <%= errors.email.msg %>
                            </span>
                         <%}%>
                        <br>
                    
                    <div class ="error"> 
                        <ul></ul>
                    </div>
                    </div>
                        <div class="main-input">
                            <label for="password">Contraseña</label>
                            <input type="password" name="password" id="password">
                        </div>
                        <% if(typeof(errors) != 'undefined' && errors.password){%>
                            <span class="text-error">
                              <%= errors.password.msg %>
                            </span>
                         <%}%>
                        <br>
                        <div class="passwordError">
                        <div class ="error"> 
                            <ul></ul>
                        </div>
                        </div>
                        <div class="recordar">
                            <label class="form-check-label">
                                <input type="checkbox" class="form-check-input" name="recordar_usuario"> Recordar usuario </label>
                        </div>
                        <div class="button-submit">
                            <button type="submit">Enviar</button>
                        </div>
                        <div class="text-end">
                            <p><a href="#">¿Olvidaste tu contraseña?</a></p>
                            <p>¿No tienes cuenta?<a href="/users/register"> Registrate</a></p>
                        </div>
                    </div>

                </form>
            </main>
            <script src="/js/users/loginValidations.js"></script>
    </body>
    <%- include('../partials/footer') %>

</html>